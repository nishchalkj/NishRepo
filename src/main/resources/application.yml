server:
      port: 8082
logging:
    level:
        org.springframework.web: DEBUG
                       
spring:
   datasource.url: jdbc:h2:mem:testdb
   data:
    jpa:
      repositories:
        bootstrap-mode: default
   h2:
    console:
      enabled: true
